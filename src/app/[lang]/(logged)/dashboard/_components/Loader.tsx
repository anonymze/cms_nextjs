"use client"

export default function Loader() {
	// const ref = useRef(null);
	// useSyncExternalStore(
	// 	register,
	// 	() => "client",
	// 	() => "server",
	// );
	
	return (
		<>
			<h1>Loader</h1>
		</>
	);
}
