model User {
    id        Int      @id @default(autoincrement())
    isActive  Boolean  @default(false)
    name      String?
    email     String   @unique
    createdAt DateTime @default(now())

    @@index(fields: [email])
}
