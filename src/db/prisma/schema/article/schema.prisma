model Article {
    id        Int     @id @default(autoincrement())
    uuid      String  @unique @default(uuid())
    title  String
    content  String
    description  String? 
    conclusion  String?

    @@index(fields: [uuid])
} 